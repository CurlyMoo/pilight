include(../../../CMakeConfig.txt)

if(${EVENTS} MATCHES "OFF")
	list(REMOVE_ITEM ${PROJECT_NAME}_sources "${PROJECT_SOURCE_DIR}/rules.c")
	list(REMOVE_ITEM ${PROJECT_NAME}_headers "${PROJECT_SOURCE_DIR}/rules.h")
endif()
